abstract and as assert base begin class const default delegate
do done downcast downto elif else end exception extern false finally fixed
for fun function global if in inherit inline interface internal lazy let
match member module mutable namespace new null of open or override private
public rec return sig static struct then to true try type upcast use val
void when while with yield

let! use! do! yield! return! match!

break checked component constraint continue fori include mixin parallel
params process protected pure sealed tailcall trait virtual

asr land lor lsl lsr lxor mod

select atomic constructor eager functor measure method object recursive
volatile not infinity infinityf nan nanf
